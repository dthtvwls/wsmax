{
  "dependencies": {
    "forever": "^0.15.3",
    "ws": "^3.0.0"
  },
  "devDependencies": {
    "phantom": "^4.0.4",
    "thor": "^1.0.0"
  },
  "scripts": {
    "start": "forever -c 'node --nouse-idle-notification --expose-gc' server.js",
    "test": "node test.js"
  }
}
